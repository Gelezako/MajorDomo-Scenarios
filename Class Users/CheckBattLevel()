if((int)gg("admin.Battlevel")<50 and (int)gg("admin.Battlevel")!="" and (int)gg("admin.atHome")==1){
 $now=round((strtotime("now")-gg("admin.CoordinatesUpdatedTimestamp")));
 if($now<60) //произносим сообщение только 1 раз
 	say("Алекс, уровенень батареи телефона ниже тридцати процентов.",2);
}

$seconds=round((strtotime("now")-gg("admin.CoordinatesUpdatedTimestamp")));
   if($seconds>20*60){ 
   		say("Данные о заряде телефона не приходят более двадцати минут",2);
   }




